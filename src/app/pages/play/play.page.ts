import { Component } from '@angular/core';

@Component({
  selector: 'app-play',
  imports: [],
  templateUrl: './play.page.html',
  styleUrl: './play.page.scss'
})
export class PlayPage {

  // On Init
  // Fetch les questions du quiz en question avec quiz.Id
  // Système d'affichage de question (Question 1 > 2)
  // Quand toutes les questions répondues, alors Résultat
}
